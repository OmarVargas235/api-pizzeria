import * as handleError from './handleError';
import * as handleSuccess from './handleSuccess';
import * as queries from './queries';
import * as utils from './utils';

export default {
    handleError,
    handleSuccess,
    queries,
    utils,
}